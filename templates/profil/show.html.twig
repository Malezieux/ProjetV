{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ profil.nom}}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ profil.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ profil.email }}</td>
            </tr>
            <tr>
                <th>Formation</th>
                <td>{{ profil.formation }}</td>
            </tr>
            <tr>
                <th>Annee_formation</th>
                <td>{{ profil.anneeFormation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profil_index') }}">back to list</a>

    <a href="{{ path('app_profil_edit', {'id': profil.id}) }}">edit</a>

    {{ include('profil/_delete_form.html.twig') }}
{% endblock %}
